<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ODIN | Mini-Message App</title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% for (let i=0; i < messages.length; i++) { %>
            <li><b><%= messages[i].text %></b> by <b><%= messages[i].user %></b> on <b><%= messages[i].added %></b> </li>
        <% } %>
    </ul>
</body>
</html>